name: ejecuta mi action  
on:
  workflow_dispatch:
  workflow_call:

jobs:
  hola:
    runs-on: ['arc-normaliza-runner-set']
    steps:
      - name: Mostrar saludo
        run: echo "Hola Mundo 🌍 desde GitHub Actions"